# ============================================
# PNPM Configuration - Turborepo Optimized
# ============================================

# Hoisting - Levanta dependências compartilhadas para o workspace root
# Reduz duplicação e melhora performance
public-hoist-pattern[]=*

# Strict peer dependencies - Garante compatibilidade
# Avisa se peer dependencies não forem satisfeitos
strict-peer-dependencies=false

# Auto install peers - Instala automaticamente peer dependencies
auto-install-peers=true

# Shamefully hoist - Para compatibilidade com ferramentas que não suportam workspaces
# Pode ser removido se não houver problemas
shamefully-hoist=false

# Node linker - Usa hoisted para melhor compatibilidade
node-linker=hoisted

# Shared workspace lockfile - Usa um único pnpm-lock.yaml na raiz
shared-workspace-lockfile=true

# Save exact versions - Evita problemas com ranges de versão
save-exact=false

# Engine strict - Força versão específica do Node se definido em package.json
engine-strict=true

# ============================================
# Performance & Cache
# ============================================

# Side effects cache - Cacheia scripts de instalação
side-effects-cache=true
