import { config } from '@workspace/eslint-config/base'

/**
 * Configuração ESLint para @workspace/core
 *
 * Este pacote contém tipos e schemas TypeScript puros (sem React).
 * Estende apenas a configuração base fortemente tipada.
 *
 * @type {import("eslint").Linter.Config}
 */
export default config
